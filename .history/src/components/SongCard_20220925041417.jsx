import { Link } from "react-router-dom";
import { useDispatch } from "react-redux";

import PlayPause from './PlayPause';
import {setActive, Play{}}

const SongCard = () => (
  <div>SongCard</div>
);

export default SongCard;
