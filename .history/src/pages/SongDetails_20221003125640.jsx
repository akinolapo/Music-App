import { useParams } from "react-router-dom";

const SongDetails = () => {
    const { songid } = useParams();

    conso

    return (
        <div>SongDetails</div>
    );
};
export default SongDetails;
